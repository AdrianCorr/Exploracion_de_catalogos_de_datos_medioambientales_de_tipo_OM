// public/js/helpers.js

// Mapa de vocabularios para traducciones
export const vocabMap = {
  es: "castellano",
  gl: "galego",
  en: "english"
};

// Aquí podrías añadir otras funciones genéricas de formateo o utilidades DOM
// por ejemplo:
// export function createElement(tag, options = {}) { ... }
